npm install
npm run dev

- FragmentLayout.jsx - Fragment-only layout
- ItemList.jsx - Mapping array to List
- Combined.jsx - Fragment + List + Total

- Card.jsx - Reusable card component
- ProductList.jsx - Map product with keys
- Section.jsx - Fragment-based section component

Notes:
- uses React Fragments (<> </>) to avoid extra wrapper divs.
- All lists use unique keys.
